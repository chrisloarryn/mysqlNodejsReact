{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "concurrently": "^5.2.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "start": "(cd api && npm run start) & (cd client && npm run start)"
  }
}